/* Problem 3

The following code differs slightly from the above code. What is the return value of map in this case? Why? */

[1, 2, 3].map(num => num * num);

// [1, 4. 9]
// result changes because there is no curly braces, so the expression will be the returned value by defult.


