/* Question 1

Will the code below raise an error?
*/

let numbers = [1, 2, 3];
numbers[6] = 5;

// No it will not.
// the 3 to 5 indexes will be empty.

// Bonus:

numbers[4]; // what will this line return?

// It will return undefined, however it is not
// really undefined, it is empty, nothing.
