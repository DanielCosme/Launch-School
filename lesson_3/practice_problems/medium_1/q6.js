/* Question 6

What do you think the following code will output? */

let nanArray = [NaN];

console.log(nanArray[0] === NaN); // False NaN is never equal in terms of itself.

// you test if is nan with the static function NUmber.isNaN()
